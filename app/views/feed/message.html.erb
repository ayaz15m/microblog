<h1>Create a new message <%= @current_user.name %></h1>

<div class="col-md-8">
  <%= simple_form_for(@message) do |f| %>
    <% f.hidden_field :user_id, :value => @current_user.id %>
    <%= f.input :message %>
    <%= f.submit class: "btn btn-primary"%>
  <% end %>
</div>
